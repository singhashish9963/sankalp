version: "3.9"

services:
  ai:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: nextstep-ai
    ports:
      - "5001:5001"
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped
